apply plugin: 'java'
apply plugin: 'eclipse'
apply plugin: 'eclipse-wtp'

defaultTasks 'clean', 'build'

repositories {
  mavenCentral()
}

configurations.all {}

dependencies {
  implementation 'org.slf4j:slf4j-simple:1.6.6'
  implementation 'commons-io:commons-io:2.4'
  implementation 'com.martiansoftware:jsap:2.1'
}
sourceSets {
    main {
        java {
            srcDir 'src/com/benglasser/headerizer'
        }
        resources {
            srcDir 'src/resources'
        }
    }
}
jar {
from configurations.runtimeClasspath.collect { it.isDirectory() ? it : zipTree(it) }
manifest { attributes 'Main-Class': 'com.benglasser.headerizer.Headerizer'}
}
